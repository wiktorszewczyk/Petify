spring:
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: gateway-service

management:
  endpoints:
    web:
      exposure:
        include: gateway,health,info